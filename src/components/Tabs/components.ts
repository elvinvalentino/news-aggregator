import styled from "styled-components";
import MuiAppBar from "@material-ui/core/AppBar";

export const AppBar = styled(MuiAppBar)`
	box-shadow: unset;
	filter: drop-shadow(0px 15px 7px rgba(0, 0, 0, 0.25));
`;
