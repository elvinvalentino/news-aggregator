import styled from "styled-components";
import Typography from "@material-ui/core/Typography";

export const NavbarBrand = styled(Typography)`
	font-size: 24px;
	font-family: "Kanit", sans-serif;
`;
